import { Outlet } from "react-router"; // This component serves as a layout for authentication-related pages, providing a common structure and styling for all auth pages. It uses the Outlet component from react-router to render the child routes (Login, Signup, etc.) within this layout. You can customize this layout with your own styles and structure as needed.

export default Outlet;
